import 'package:flutter_test/flutter_test.dart';

void main() {
  group('EventStore Tests', () {
    test('should initialize', () {
      expect(true, true);
    });

    test('should append event', () {
      expect(true, true);
    });

    test('should generate proof pack', () {
      expect(true, true);
    });
  });
}
