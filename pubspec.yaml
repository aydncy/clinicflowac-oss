name: clinicflowac
description: ClinicFlowAC OSS Demo
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State / utilities
  flutter_riverpod: ^2.5.1
  http: ^1.2.1

  # Persistent storage (Flutter 3.22.3 compatible)
  hive_ce: ^2.19.3
  hive_ce_flutter: 2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
